" .vimrc file for Fernando Hernandez

" don't allow vim to try to conform to vi
set nocompatible

" enable syntax and plugins
syntax enable
filetype plugin on

set tabstop=8 " set tabstop
set softtabstop=4 " set softtabstop
set expandtab

set number " show line numbers
